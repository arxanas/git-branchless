name: Nix on macOS

# Currently failing -- see https://github.com/arxanas/git-branchless/pull/159#issuecomment-958611320
on: {}
#   schedule:
#     # Run once every day at 6:40AM UTC.
#     - cron: "40 6 * * *"

#   push:
#     branches:
#       - master

#   pull_request:
#     branches:
#       - "ci-*"

jobs:
  nix:
    runs-on: macos-latest
    name: nix-build
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: cachix/install-nix-action@v14.1
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
      - run: nix build --print-build-logs --show-trace --override-input nixpkgs github:NixOS/nixpkgs
